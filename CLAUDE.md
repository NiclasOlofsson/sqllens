# CLAUDE.md — sql-dialect-grammars

Guidance for Claude Code working in this repository. (Folder name is provisional — rename freely.)

## What this project is

**Generate TypeScript SQL parsers we can use in our own projects**, built from open, split **ANTLR4 grammars (`.g4`)** that we fork or hand-author and run through the **antlr4ng** toolchain. Two kinds of target:
- **Fork-and-clean** — dialects with a good existing `.g4` we fork and conformance-validate: **Databricks** (from apache/spark), **T-SQL** and **Snowflake** (from grammars-v4 — the Snowflake one was long assumed not to exist; it does, and is actively maintained — corrected 2026-06-10), and **BigQuery** (from `bytebase/parser` `googlesql/`, BSD-3 — corrected 2026-06-13; the earlier "no open grammar" claim was wrong, mirroring the Snowflake correction).
- **Hand-author** — the one proprietary cloud warehouse with **no authoritative open grammar**: **Redshift**.

**The primary deliverable is the generated TypeScript parsers** — a `parse(sql, dialect)` library we consume in our own projects, TS-native with no Python or Java at runtime. The `.g4` grammars are the *means*: forking or hand-authoring them is how we build and maintain those parsers, and we optimize for a working parser over grammar elegance. The grammars are a useful by-product — for the warehouse dialects an open spec that doesn't exist today, and where we improve a grammars-v4 grammar we contribute the fix back upstream — but they are not the main delivery.

**Primary usage — this exists to power editor/IDE features (decided 2026-06-13).** The "own projects" that consume it are first and foremost **language tooling**: an **LSP** (diagnostics, hover types, go-to-definition, completion, document symbols) and the **SQL debugger**, both in the sibling `dbt-studio-vscode` extension. So the semantic layer (`scope → qualify → infer → lineage → symbols`) is **central, not a Databricks-only add-on**, and editor-shaped requirements are first-class design drivers — judge designs by "does this serve an LSP/editor consumer": **positional parse diagnostics** (line/column for squiggles, not a bare error count — issue #6), **small per-dialect load** (an extension bundles one dialect, not all four — issue #8), and stable, immutable analysis results that survive incremental edits. Non-editor / batch programmatic use is supported but secondary; when a design choice trades off, favor the editor/LSP consumer.

**The parser is syntax only** — it produces a parse tree. On top of it, for Databricks, we *are* building a **semantic layer** (name resolution = scope, plus schema-fed qualify) because the consumers (editor support, the SQL debugger) need it — Nicke directed this 2026-06-06. **Out — actually Nicke-cleared:** SQL transpilation (Nicke: "i dont care at all about the transpile", 2026-06-06); and **object DDL** — CREATE/ALTER/DROP-style object management (UC object DDL, column MASK/ROW FILTER, Python UDF bodies) — "what we don't do is regular DDL" (Nicke 2026-06-10, refining the broader same-day clearance). **Open, likely future scope (NOT Out):** the operational non-SELECT statements a data engineer runs outside dbt — COPY INTO, Delta maintenance (OPTIMIZE/VACUUM/RESTORE/…), GRANT, UPDATE/DELETE/MERGE modelling depth (Nicke 2026-06-10: "might become in scope … we do it occasionally"). Both sets are pinned at their current support level in `tests/databricks.doc-coverage.test.ts`. **NOT cleared — open, do NOT treat as Out:** **type inference** and **column lineage**. These were carried over from the origin session's "syntax only — no semantic analyzer" framing (which also listed scope resolution + function signatures); Nicke overrode that framing on 2026-06-06 by directing the scope + qualify semantic layer, and never separately cleared type inference or lineage. A prior edit wrongly stamped them "Nicke-cleared" — that was the assistant's assumption, not Nicke's decision. Type info is in fact needed to finish schema-fed validation (e.g. struct-field existence — see [docs/PLAN.md](docs/PLAN.md) Open Gaps); lineage was only ever noted "revisit later" (deferred, rides on qualify), not cut. **Expression modelling** (`a+b`, CASE, calls, aggregates, window/`OVER`, `GROUP BY`/`HAVING`, predicates, lambdas, subscript, the `date_add`/`CURRENT_*` special forms) — **built 2026-06-06**, **corpus-complete: every expr node in all 1558 models lowers to a typed `Expr` — 0 `other`, gated by `tests/ir-completeness.test.ts`**. GROUP BY captures every key (incl. inside ROLLUP/CUBE/GROUPING SETS); qualified `t.*` expands only its source; `aggregate` uses a comprehensive Spark aggregate-name set. Was never a descoping.

## Origin / design rationale — READ THIS FIRST in a fresh session

This project was designed in a long discussion in the **sibling workspace** `c:\Development\github\dbt-studio-vscode` on **2026-06-05**. The full reasoning — why ANTLR/`.g4`, why hand-authored, why these dialects, why a sqlglot conformance oracle, the dbt-Fusion open-source investigation that motivated all of it, the TS-vs-Go/Rust porting discussion — lives in that session's transcript:

```
C:\Users\nicke\.claude\projects\c--Development-github-dbt-studio-vscode\0ef58efb-c91e-4bee-ac6f-9d8fa3c2d45d.jsonl
```

It is JSONL (one message per line). To recover context, read it or grep it for: `ZetaSQL`, `bigqueryuntyped`, `SqlBaseParser`, `antlr4ng`, `sqlglot`, `Fivetran`, `T__0`.

Related durable notes from that workspace:
```
C:\Users\nicke\.claude\projects\c--Development-github-dbt-studio-vscode\memory\MEMORY.md
  → especially project_dbt_grammars_not_oss.md
```

The development plan is at **[docs/PLAN.md](docs/PLAN.md)** — start there.

## Current status

Current state only, verified 2026-06-10 (all suites green at last full run). History lives in `git log` and the PLAN.md Open Gaps entries — don't append changelog narrative here; keep this section true to *now*.

- **Phase 0 done; Databricks, T-SQL, Snowflake and BigQuery are at their gates; the Phase 1.5 semantic layer is built and wired (and runs unchanged on all four).** Next: the remaining warehouse dialect — Redshift (hand-authored; PLAN.md Phases 3/5 still hold stale Redshift-first detail, pending a re-sequence).
- **BigQuery (GoogleSQL)** — forked `bytebase/parser` `googlesql/` (BSD-3, vendored at `grammars/bigquery/`); the initial port was the Go-target embedded code (49 `NotifyErrorListeners` error actions + 7 `localctx`/`:=`/`GetStop()` predicate blocks → the antlr4ng TS API). It then got a large grammar build-out to reach the full GoogleSQL/ZetaSQL surface (grammar transcribed from `google/googlesql` `googlesql/parser/googlesql.tm`, the live Textmapper grammar — the old `bison_parser.y` is gone): **pipe query syntax** (`|>` — every operator, FROM-queries, subpipelines, GA in BigQuery), **graph/GQL** (`GRAPH_TABLE(…)` in FROM, the `GRAPH …` statement, MATCH/patterns/quantifiers/path-modes/search-prefixes, CALL/YIELD/PER, `CREATE/DROP PROPERTY GRAPH`, graph subqueries `VALUE{}`/`EXISTS{}`/`ARRAY{}`, `IS SOURCE/DESTINATION/LABELED`), **chained function calls**, **braced/proto/struct constructors** (`{f:v}`, `STRUCT{}`, `NEW T{}`), **MATCH_RECOGNIZE** (quantifiers/anchors/AFTER MATCH SKIP/OPTIONS), **FOR UPDATE**, **LATERAL**, **MAP type**, `CREATE/ALTER/DROP SEQUENCE`, `LIMIT ALL`, `ALTER … SET GENERATED`, and the **DOT_IDENTIFIER** rule (reserved keywords as path components after a dot — `t.array`, `hll_count.merge`, `@@FROM`). A batch of upstream port bugs were fixed along the way (TVF-call paren, AT-TIME-ZONE keyword, CORRESPONDING/BY NAME, bare QUALIFY, `braced_constructor` opening with `}` not `{`, `cube_list` missing its first expr, USING dotted-path vs comma-list, the `>>` token swallowing nested-generic closers). Entry is `root` (`stmts EOF`). `lower` covers the query layer onto the shared IR (projections incl. `SELECT * EXCEPT/REPLACE` and `t.*`, table/subquery/UNNEST-lateral sources, FROM-queries and `TABLE name` → modelled selects, join chains + ON, WHERE/GROUP BY incl. ALL + ROLLUP/CUBE/GROUPING SETS keys/HAVING/QUALIFY, CTEs incl. RECURSIVE, UNION/EXCEPT/INTERSECT, ORDER BY/LIMIT, the left-recursive ZetaSQL expression grammar); **pipe queries (`|>`) and graph/GQL (`GRAPH_TABLE`/`GRAPH`) now lower to faithful first-class IR** — a `PipeExpr` (base input + one `PipeStage` per operator, all 31 modelled, each keeping its `|> OP` span; a corpus gate proves the `other` drift-guard never fires) and a `GraphTableSource` (graph name + MATCH element variables with labels/direction + WHERE + COLUMNS/RETURN outputs). The semantic layer **flows** the relation through pipe stages and binds graph element variables (new `relation`/`graphtable` resolved-source kinds in `src/scope`), so a column ref in `|> WHERE x` or `(a)-[e]->(b)` resolves at that point and output columns propagate (WHERE keeps, SELECT replaces, EXTEND adds, DROP/RENAME/SET transform, AGGREGATE = aggs+keys, JOIN unions). Remaining unmodelled forms (object DDL) become `other`/flagged non-query bodies; a valid parse never throws. The semantic layer runs **unchanged across dialects** — Databricks (Spark 4.0 `|>`) lowers onto the *same* PipeExpr IR (`bigquery.pipeline.test.ts`, `bigquery.pipe.test.ts`, `databricks.pipe.test.ts`, `bigquery.graph.test.ts`). Gates (`bigquery.corpus.test.ts` analyzer + `bigquery.parser-corpus.test.ts` parser, skipIf-absent): the **ZetaSQL `.test` golden corpora** extracted via `tools/extract-googlesql-tests.mjs` / `extract-googlesql-parser-tests.mjs` (gitignored, sparse-cloned `vendor/googlesql/`, now resolved through the `sqllens-corpus` repo); the extractors are **mode-aware** (drop `type`-mode, wrap `expression`-mode as `SELECT (…)`), **classify each `{{…}}` alternation variant by its own ALTERNATION GROUP expected**, and share a per-case classify-out (feature-off / divergence / detect-only DDL / post-parse-structural errors the bare parser accepts) so both corpora grade identically. Both are **two-sided over the IN-SCOPE bucket** (object DDL / DEFINE MACRO / empty-script excluded symmetrically): analyzer in-scope positives parse at **14,695/14,714 (99.9%)** with **166/166 syntax-error negatives rejected (0 accepted)**; the stricter parser-testdata gate is **2,662/2,662 positives and 2,035/2,035 negatives (0 accepted)**; `lower`+`resolveScopes` are proven total (no-throw) over every parsed positive. Inference registry is seeded (~190 doc-typed GoogleSQL functions; expands over time). **Open gaps** (~19 in-scope unparsed positives): pipe `AGGREGATE WITH DIFFERENTIAL_PRIVACY`, multi-level aggregation `agg(x GROUP BY …)`, TVF `TABLE`/scalar args, `WITH POSITION` on param-table sources, and a few mis-bucketed ZetaSQL errors; expression coverage not wired into `ir-completeness`.
- **Databricks** — the Spark-fork grammar generates and typechecks clean; entry is `compoundOrSingleStatement` (single statements + BEGIN…END scripting compounds, which lower as one flagged body), with the Databricks-only `t@v123` time-travel shorthand added to the grammar. Inline-table bodies (`VALUES`, `INSERT … VALUES`, `TABLE t`) lower to modelled selects — a valid parse never throws. **Spark 4.0 pipe syntax (`|>`)** lowers onto the shared `PipeExpr` IR (unwinding Spark's left-recursive `queryTerm |> rhs` into base + ordered stages), so the semantic layer flows columns through pipe stages identically to BigQuery (`databricks.pipe.test.ts`). Gates: **1558/1558** Oatly models parse with zero syntax errors (`databricks.local-coverage`); the semantic corpus suites (`scope.corpus`, `ir-completeness`, `conservation`, `lineage`, `symbols`) run the full pipeline over all 1558 models with 0 throws; expression modelling is corpus-complete — 0 `other` nodes; `databricks.doc-coverage` pins per-construct support against the official reference (the query layer is complete; object DDL is `noparse` by cleared scope, the operational statements — COPY INTO, Delta maintenance, GRANT — are `noparse`/`nonquery` open gaps, likely future scope). The **scraped SQL-language-manual corpus** — every example from the 1,114 docs.databricks.com language-manual pages via `tools/scrape-databricks-docs.mjs` (gitignored `harness/local/databricks-docs/`, ~4,085 files, Spark `>`-prompt result rows stripped) — gates `databricks.docs-corpus`. The gate **ratchets on the in-scope query bucket only** (SELECT/WITH/VALUES/…) and *reports* the dml/ddl buckets without gating them, since object/platform DDL is cleared Out; the gate now requires **100% of the in-scope query bucket** (KNOWN_BAD + issue-#4 gaps excluded; documented floor 3,062; the grammar is the Spark grammar) and reports the dml/ddl buckets without gating them (cleared-Out Unity Catalog object management + operational Delta maintenance). Not touched this session. Statement-kind detection is now real (`src/ir/statement.ts` + each dialect's `lower()`; `resolveScopes` surfaces it on `ScopeTree`); the T-SQL docs gate buckets from the parse, while this Databricks gate (and Snowflake's) still split in/out with the `tests/helpers/sql-kind.ts` leading-keyword regex — migrate them the same way when touched.
- **T-SQL** — parse + lower onto the same shared IR; the semantic layer runs **unchanged** on it (`tsql.test.ts`, mutation-verified). `parseTSql` is **EOF-anchored** (trailing input is an error, never silently dropped). `lower` covers the core query language plus PIVOT/UNPIVOT, APPLY, OPENJSON/OPENXML (columns from their `WITH` schema), TVFs and XML `.nodes()` (opaque sources), OPENQUERY, non-ANSI `*=`, TOP/OFFSET-FETCH (IR `limit`), IS [NOT] DISTINCT FROM, and the 2022 WINDOW clause (`OVER w` resolves named windows, chained, cycle-guarded); FOR SYSTEM_TIME and TABLESAMPLE parse. CLR/spatial static + instance methods (`geography::STGeomFromText(…)`, `point.STDistance(o).STBuffer(1)`) and the query OPTION hints (`USE HINT (…)`, `QUERYTRACEON n`) parse — added from the docs corpus. Gates: the grammars-v4 137-file example corpus parses **135/137** (the 2 failures are upstream gaps; that corpus is mostly DDL, so grammar-conformance only); the **scraped MS docs corpus** — every ` ```sql ` example from MicrosoftDocs/sql-docs `docs/t-sql` via `tools/extract-tsql-docs.mjs` (gitignored `harness/local/tsql-docs/`, ~3,400 files) — gates `tsql.corpus` at **100% of the in-scope query bucket** (parse-derived buckets; KNOWN_BAD excluded; documented floor 854 in `tests/tsql.corpus.test.ts`); bucketing is **parse-derived** (per-statement `statementCategories`, first substantive statement decides; the `sql-kind.ts` regex is only the no-parse fallback), with 14 documented-broken examples excluded via `tests/tsql-corpus-known-bad.ts` (each verified against the source markdown — NNBSP whitespace, an unterminated string, fragments, reserved-word identifiers, one documented-to-fail example) and 3 mixed scripts whose payload is out-of-scope DDL/admin reclassified (`OUT_OF_SCOPE`, same file); the 2025/Fabric/Synapse query surface added for that gate (`tests/tsql.conformance.test.ts`, every case doc-cited): JSON constructors/aggregates (`RETURNING`, `JSON_ARRAYAGG`/`JSON_OBJECTAGG`), `REGEXP_LIKE` predicates, `CURRENT_DATE`, `TRIM(LEADING|TRAILING|BOTH …)`, generic-function `OVER` (PRODUCT), graph `MATCH`/`SHORTEST_PATH`/`$node_id`-family/`FOR PATH`/`GRAPH PATH`, `VECTOR_SEARCH`/`AI_GENERATE_*`/`PREDICT`, both `OPENROWSET` forms + `OPENDATASOURCE` part-skipping, `OPTION (LABEL | FOR TIMESTAMP AS OF | {FORCE|DISABLE} EXTERNALPUSHDOWN | FORCE … PLAN)`, `REDUCE|REPLICATE|REDISTRIBUTE(n)` join hints, `GROUP BY ()`/`ROLLUP()`/legacy `WITH ROLLUP|CUBE`, `TOP … WITH APPROXIMATE`, backslash line continuation in constants; `tsql.adventureworks` — a 71-table catalog + 20 `CREATE VIEW` bodies, **all 20 parse and resolve with 0 unknown-table/-column**; `tsql.doc-coverage` pins per-construct support against the MS reference (remaining `noparse`: BULK INSERT, temporal DDL, GRANT permission lists/DENY/REVOKE, ODBC escapes). The Oatly corpus is Databricks dialect — **not** a T-SQL gate (only ~14% parses as T-SQL, by design).
- **Snowflake** — forked grammars-v4's `sql/snowflake` (at `923a1a9`) and cleaned against the official reference, every fix doc-cited and TDD'd (`tests/snowflake.test.ts`): window frames (upstream had them commented out), `SELECT *` ILIKE/EXCLUDE/REPLACE/RENAME, `$$` strings as expressions, real MATCH_RECOGNIZE patterns (upstream had a DUMMY stub), WITHIN GROUP on ordered-set aggregates, multi-row VALUES, structured `OBJECT(…)`/`MAP(…)`/FILE types, quoted-keyword strings (`'CSV'` &c. lexed as option tokens, unusable as strings), ALTER SESSION with any documented parameter, `!method()` calls, ICEBERG tables, stage queries in FROM, `IDENTIFIER('…')`. `lower` covers the query layer onto the shared IR (QUALIFY, star modifiers, UNION BY NAME, FLATTEN→lateral with the six documented columns, PIVOT/UNPIVOT, variant `:` paths→subscript, inline VALUES→modelled selects, `$n` refs); the semantic layer runs **unchanged** (`snowflake.pipeline.test.ts`). Gates (`snowflake.corpus.test.ts`, skipIf-absent): grammars-v4's 51-example corpus at **100%**; the **scraped docs corpus** — every SQL example from all 2,348 docs.snowflake.com sql-reference pages via `tools/scrape-snowflake-docs.mjs` (gitignored `harness/local/snowflake-docs/`, ~5,832 files) — the gate now requires **100% of the in-scope query bucket** (KNOWN_BAD excluded; documented floor 2,942) and reports the dml/ddl buckets without gating them (this corpus is ~46% platform DDL + standalone Scripting, out of scope). Driven there 2026-06-12 by a run of "wave" commits (96.3% → … → 100%); not touched this session. Remaining items: `docs/snowflake-backlog.md`.
- **Type inference is per-dialect** — the engine (`src/infer/infer.ts`) is dialect-agnostic; per-dialect knowledge lives in `src/infer/dialect.ts` (registry) with the rule tables in `src/infer/functions.ts` / `src/infer/snowflake.ts`: function registries, literal rules, scalar-type aliases (bit→boolean, datetime→timestamp, nvarchar→string, float→double, NUMBER→decimal), and per-dialect division (Spark `int/int`→double, T-SQL `int/int`→int, Snowflake `int/int`→decimal — a three-way `division` strategy). The dialect rides as a string tag on `Scope` (`resolveScopes(query, "tsql")`), so qualify/symbols needed no change. The registries are built from the vendor references with doc-fetched return types (Spark ~520 entries incl. the H3/ST-geospatial/AI/IP/VARIANT/TIME families; T-SQL ~210 incl. the 2022/2025 additions; Snowflake ~300 incl. the semi-structured/AS_*/context/geospatial families); a rule is absent only where the documented return type is argument-value-dependent or unstated. Gates: `tsql.infer.test.ts`, `infer.registry.test.ts`, `snowflake.pipeline.test.ts`.
- **Genuine boundaries / open gaps** (tracked, not descoped — full list in [docs/PLAN.md](docs/PLAN.md) Open Gaps): XML `.nodes()`/`.value()` column extraction needs an XML-shredding subsystem (its own feature); OPENJSON/OPENXML `WITH`-column *types* aren't captured (names only); a larger T-SQL SELECT corpus (e.g. sqlglot's `test_tsql.py` cases) would harden `lower` and type coverage; correctness is self-graded — the corpus gates prove "no throw" plus stats our own code computes (the doc-coverage suites pin support *levels*, not expected semantic outputs).
- **Vendored inputs (all gitignored):** `vendor/grammars-v4` — sparse clone of `sql/`, SHA `923a1a9`; `vendor/spark` — apache/spark's `SqlBase*.g4` (the Databricks fork base); `vendor/adventureworks/instawdb.sql` — downloaded MS sample; `harness/local/databricks` — the Oatly corpus (proprietary, machine-local); `harness/local/snowflake-docs` — the scraped Snowflake docs-example corpus; `harness/local/databricks-docs` — the scraped Databricks SQL-language-manual corpus; `vendor/sql-docs` — sparse clone of MicrosoftDocs/sql-docs `docs/t-sql` for the T-SQL docs corpus; `vendor/googlesql` — sparse clone of google/googlesql `googlesql/analyzer/testdata` (the ZetaSQL `.test` golden files); `harness/local/bigquery-zetasql` — the positive/negative SQL corpus extracted from them (all docs/test-derived; rebuild with the `tools/{scrape-snowflake-docs,scrape-databricks-docs,extract-tsql-docs,extract-googlesql-tests}.mjs` scripts).

## Locked decisions (don't relitigate without a new reason)

- **Format:** ANTLR4, **split** grammars per dialect — a `lexer grammar <Dialect>Lexer` + a `parser grammar <Dialect>Parser`. Split is required for lexer **modes** (dollar-quoting, embedded JS/Python UDF bodies) and avoids ANTLR's anonymous `T__n` tokens.
- **TS generation:** ANTLR4 TypeScript target + the **antlr4ng** runtime (actively maintained by Mike Lischke; ~9–35% faster than the older, semi-abandoned antlr4ts). **Verified 2026-06-06** on both Databricks and T-SQL: the pure-TS **`antlr-ng` CLI works — no Java/jar needed**. Command: `npx antlr-ng -D language=TypeScript -o src/generated/<dialect> grammars/<dialect>/*.g4` — antlr-ng **defaults to a Java target**, so `-D language=TypeScript` is required. Versions: antlr-ng 1.0.10, antlr4ng 3.0.16. Generated `.ts` uses `.js` ESM imports and runs under vitest with `moduleResolution: Bundler`. (Jar fallback `antlr-4.13.x-complete.jar -Dlanguage=TypeScript` no longer needed.)
- **Build / typecheck compiler:** **TS7 native (`tsgo`, `@typescript/native-preview`)** — adopted 2026-06-06 to experiment ahead of GA. Measured ~2.2× faster than TS6 `tsc` on our generated code (1.8s vs 4.0s; gap widens as dialects are added). `tsc` (TS 6.0.3) stays installed as fallback. `npm run typecheck` runs `tsgo`. **Revisit at packaging:** validate `.d.ts` emit from `tsgo` before shipping — `.ts→.d.ts` is at parity in the 7.0 beta but has tracked intentional diffs (typescript-go#989); fall back to `tsc` for the declaration step only if its output differs.
- **Architecture:** **no shared "core" grammar and no inheritance.** "Core SQL" is a concept (dialects share an ANSI-ish ancestry), not an artifact we build — and ANTLR `import` doesn't compose cleanly anyway. Each dialect is a **standalone pair of split `.g4` files**, forked from its best starting point and edited in place (how dbt/Calcite/everyone does it):
  - **Databricks** ← fork **apache/spark's `SqlBaseLexer.g4`/`SqlBaseParser.g4`** (vendored at `vendor/spark/`), renamed to `DatabricksLexer`/`DatabricksParser`. **Switched 2026-06-06** from grammars-v4's `sql/databricks`: that grammar was light (~30 KB) and incomplete; Spark's is the complete, authoritative grammar (Databricks SQL = Spark SQL) at the cost of a one-time port of its embedded Java `@members`/actions to TypeScript. The port: `getText()`→`this.text`, `_input`→`this.inputStream` (LA returns a char code), `Deque<String>`→`string[]`, every action/predicate prefixed with `this.`, and a `@header` import so the parser's `complexDataType` action can cast the token source to the lexer. Spark relies on a runtime `UpperCaseCharStream` for case-insensitive keywords, which we don't have — replaced with ANTLR's `options { caseInsensitive = true; }` on the lexer. Entry rule is Spark's `singleStatement` (one statement + EOF), not the old `databricks_file`.
  - **T-SQL** ← fork grammars-v4's `sql/tsql` (`TSqlLexer.g4`/`TSqlParser.g4`, mature, ~200 KB parser).
  - **Snowflake** ← fork grammars-v4's `sql/snowflake` (`SnowflakeLexer.g4`/`SnowflakeParser.g4`, ~190 KB split pair, maintained by its original author + Bytebase since 2022). **Corrected 2026-06-10:** this repo's earlier claim that no grammars-v4 Snowflake grammar exists was wrong — assessed against the official docs, the grammar earned a fork the same way `sql/tsql` did (no embedded actions, `caseInsensitive` lexer, generated TS first try); the gaps it did have (window frames commented out, star modifiers, MATCH_RECOGNIZE stub, …) were fixed in our fork.
  - **BigQuery** ← fork **`bytebase/parser` `googlesql/`** (`GoogleSQLLexer.g4`/`GoogleSQLParser.g4`, BSD-3, vendored at `grammars/bigquery/`). **Corrected 2026-06-13:** the earlier "no open grammar exists" claim was wrong (same kind of miss as Snowflake). The only work to make it generate as TS was porting the Go-target embedded code (error actions + `localctx`/`:=` predicates) to the antlr4ng API; `caseInsensitive` lexer, generated TS first try. Entry rule is `root` (`stmts EOF`).
  - **Redshift** ← hand-authored standalone (no open grammar exists); crib structure from grammars-v4 `sql/snowflake`, `sql/trino` (and Postgres, its base).
- **Validation:** a conformance harness that parses a per-dialect **known-good corpus** (our own compiled SQL, plus grammars-v4 `examples/` only where they're substantive — they were dropped for Databricks as near-empty stubs) and requires the generated parser to parse it all with **zero syntax errors**. For Databricks the corpus is the real Oatly compiled-dbt SQL (`harness/local/databricks`). No Python in the dev/CI loop, and no sqlglot/oracle cross-check (dropped 2026-06-06). Harness shape mirrors `dbt-studio-vscode/experiments/native-sql-parser-v7/harness`.
- **Dialect order (reordered 2026-06-06; Snowflake pulled forward 2026-06-10; BigQuery pulled forward 2026-06-13):** **Databricks** (≈ Spark SQL → forked apache/spark's `SqlBase*.g4`, fastest to first green: 100% on the Oatly corpus) → **T-SQL** (fork grammars-v4's mature `sql/tsql`) → **Snowflake** (fork grammars-v4's `sql/snowflake`) → **BigQuery** (fork `bytebase/parser` `googlesql/` — pulled ahead of Redshift once that fork base proved viable, same as Snowflake) → then the one hand-authored dialect: **Redshift**. Forkable starting points go first; Redshift remains the original motivation, sequenced last.

## Sources of truth, per dialect

- **Authoritative syntax:** the dialect vendor's official SQL manual. Always wins.
- **Per-dialect deltas:** sqlglot's dialect files (`databricks.py`, `tsql.py`, `snowflake.py`, `bigquery.py`, `redshift.py`) — the curated "what's different from base SQL." Read as a reference only (we don't run sqlglot or use it as an oracle).
- **Vocabulary / reserved-word checklist:** dbt's generated lexer token lists in `dbt-labs/dbt-core` → `crates/dbt-sql/dbt-lexer-*/.../*Lexer.tokens`.
- **Reference / fork-base grammars** (`antlr/grammars-v4`, BSD/MIT — forkable): `sql/tsql` (mature, split — the **fork base for T-SQL**), `sql/databricks` (light, split — **cross-reference** for Databricks), plus `sql/snowflake`, `sql/trino` to crib structure. No BigQuery or Redshift there — but BigQuery's fork base is **`bytebase/parser` `googlesql/`** (BSD-3), outside grammars-v4. No open Redshift grammar anywhere.
- **BigQuery ground truth:** Google's **ZetaSQL / GoogleSQL** — the syntax *spec* is now `google/googlesql` `googlesql/parser/googlesql.tm` (a Textmapper grammar; the old C++ Bison `.y` was removed) — read it for exact productions, don't port it wholesale; and the **conformance corpus**: the 377 `googlesql/analyzer/testdata/*.test` golden files drive the two-sided `bigquery.corpus.test.ts` gate (positives must parse, syntax-error negatives must be rejected). The grammar we actually fork is Bytebase's ANTLR port of GoogleSQL, extended toward `googlesql.tm`, not ZetaSQL itself.

## Commands

```bash
npm run gen -- <dialect>             # antlr-ng → TS into src/generated/<dialect>/ (databricks | tsql | snowflake | bigquery); dialect arg required
npm run typecheck                    # tsgo -p tsconfig.json (noEmit; tsc is the fallback compiler)
npm test                             # vitest run — all suites, including the corpus gates
npx vitest run tests/tsql.test.ts    # one test file
npx vitest run -t "expands t.*"      # one test by name
npm run format                       # prettier --write . (format:check for CI-style check)
```

There is no `build` script (the library is consumed as TS; emit is a packaging-phase question) and no separate harness runner — the conformance corpora run as vitest suites. `src/generated/` is gitignored: run `npm run gen -- <dialect>` for each of databricks/tsql/snowflake/bigquery after a fresh clone or any `.g4` edit, or every test fails at import.

**Corpus location (`SQL_CORPUS_DIR`).** The corpus data (proprietary Oatly SQL + scraped closed-license docs + bulky upstream clones) is too large / partly proprietary to commit here. It lives in **one place**: the private repo `sqllens-corpus`, cloned to a sibling folder. There is **one way** to find it — the env var `SQL_CORPUS_DIR`, set in a committed `.env` at this repo's root (`SQL_CORPUS_DIR=C:/Development/github/sqllens-corpus`). No fallbacks: no sibling-folder guessing, no in-tree copy. The resolver lives in two trivial twins — `tests/helpers/corpus.ts` (vitest) and `tools/corpus-paths.mjs` (node scripts) — each reads `process.env.SQL_CORPUS_DIR` (real env wins, for CI), else parses it from `.env`, else **throws**. Both expose `corpusPath(rel)` = `resolve(SQL_CORPUS_DIR, rel)`, where `rel` is the path the data has inside the corpus repo (`harness/local/databricks`, `vendor/sql-docs/docs/t-sql`, …). The corpus repo mirrors those relative paths. The scraper/extractor tools (`tools/{scrape,extract,harvest,clean}-*.mjs`) also resolve their SRC/OUT through `corpusPath`, so they read and write the corpus repo directly — there is no in-tree copy to keep in sync. `.env` is **committed** so every worktree and fresh clone resolves the same path with zero setup; a new machine edits the one line. Keep `sqllens-corpus` **private**: it holds the proprietary Oatly corpus and the closed-license scraped docs (Databricks/Snowflake/Redshift), which must not be redistributed. (`tools/gen.mjs` is unaffected — it reads `grammars/<dialect>`, which stays in this repo.)

The corpus gates `describe.skipIf` their corpus directory away when absent, so they're a no-op on machines without the data:

- `harness/local/databricks` (gitignored, 1558 Oatly compiled-dbt files) — gates `databricks.local-coverage` (parse, 100% required), `scope.corpus`, `ir-completeness`, `conservation`, `lineage`, `symbols`.
- `harness/local/databricks-docs` (gitignored; rebuild with `node tools/scrape-databricks-docs.mjs`) — gates `databricks.docs-corpus` (ratchet, pass count must never drop below the pinned baseline).
- `vendor/grammars-v4/sql/tsql/examples` (gitignored sparse clone) — gates the `tsql.corpus` grammars-v4 example gate; `harness/local/tsql-docs` (gitignored; rebuild with `node tools/extract-tsql-docs.mjs`) — gates the `tsql.corpus` MS-docs ratchet.
- `vendor/adventureworks/instawdb.sql` (gitignored download) — gates `tsql.adventureworks` (schema-fed resolution).
- `vendor/grammars-v4/sql/snowflake/examples` (same sparse clone) — gates `snowflake.corpus` (100% required).
- `harness/local/snowflake-docs` (gitignored; rebuild with `node tools/scrape-snowflake-docs.mjs`) — gates the `snowflake.corpus` docs-ratchet (pass count must never drop below the pinned baseline).
- `harness/local/bigquery-zetasql` (gitignored; rebuild with `node tools/extract-googlesql-tests.mjs`, needs the `vendor/googlesql` sparse clone) — gates `bigquery.corpus` (two-sided: positive ratchet + negative-rejection ratchet + a `lower`/`resolveScopes` no-throw sweep).

A green run with a corpus absent proves less than it looks like — check the skip count before claiming the gate passed.

Observed once (2026-06-10, Windows): a full `npm test` collapsed in ~5s with every file failing `Cannot read properties of undefined (reading 'config')` and "no tests" — a vitest worker-pool crash, not real failures (immediate rerun was green, 228/228). If you see that exact signature, rerun before debugging anything.

## Code map — the pipeline

`parse → lower → resolveScopes → qualify → infer / lineage / symbols`. Only the first two stages are per-dialect; everything after operates on the shared IR and runs unchanged on every dialect.

```
grammars/<dialect>/        split .g4 pair — the hand-maintained source
src/generated/<dialect>/   antlr-ng output (gitignored build product; never hand-edit)
src/<dialect>/parse.ts     parse wrapper: two-stage SLL→LL with BailErrorStrategy, returns CST + error count
src/<dialect>/lower.ts     CST → IR; the only place that knows the dialect's parse-tree shape
src/ir/ir.ts               dialect-neutral IR (QueryExpr/SelectExpr/SetOpExpr/PipeExpr/Source/Expr…; PipeExpr = base + ordered PipeStage[]; GraphTableSource for GQL); every node keeps a `cst` back-ref for source spans
src/scope/scope.ts         resolveScopes(query, dialect) — schema-free symbol table: visible sources, CTE resolution, output columns; the dialect string rides on Scope
src/qualify/               Schema (sqlglot-style mapping) + qualify — `*` expansion, unknown-table/column/field diagnostics, bottom-up column types
src/sema/resolve.ts        shared schema-aware column→source binder used by infer + lineage (local-first, then correlation to enclosing scopes)
src/infer/                 inferType — engine in infer.ts is dialect-agnostic; per-dialect knowledge in dialect.ts (Spark/T-SQL rules in functions.ts/literals.ts/types.ts, Snowflake's in snowflake.ts, coercion in coerce.ts)
src/lineage/               lineage/originsOf — base-table origins per output column
src/symbols/               deriveSymbols — kind×modifier symbol model over the scope tree; carries types/origins when given a schema
src/index.ts               public API (Databricks exported; T-SQL, Snowflake and BigQuery parse/lower exist but aren't exported yet)
tools/gen.mjs              generation driver (sorts .g4 so the lexer generates before the parser — tokenVocab)
```

Adding a dialect touches four places: `grammars/<dialect>/`, `src/<dialect>/parse.ts` + `lower.ts`, and one `InferDialect` entry in `src/infer/dialect.ts`. A missing function rule in a registry yields `unknown`, never a wrong type — that's the contract; don't guess return types.

## Conventions

- One folder per dialect: `grammars/<dialect>/<Dialect>Lexer.g4` + `<Dialect>Parser.g4`. No `grammars/core/` — every dialect grammar is standalone.
- Generated TS (`src/generated/`) is **build output** — gitignored, regenerated by `npm run gen`. Never hand-edit it.
- Every dialect-specific grammar rule gets a comment linking the manual section that justifies it.
- The conformance corpus is the gate: a grammar change that regresses the corpus is **not done**. TDD for grammars = add corpus case → harness fails → edit grammar → harness green → commit.
- Match this file's decisions; if a decision turns out wrong, update this file in the same change that departs from it.
- **No descoping core work without Nicke's explicit clearance.** Cutting a real part of the job (e.g. expression modelling) out of scope is a decision only Nicke makes, stated out loud at the time. If something is too big to finish now, it stays a **visible Open Gap** that keeps answering "what's left" — never a quiet scope boundary, a "v1 doesn't do X", or an IR/code comment that removes it from view. Incomplete is fine; silent is not. **But visible is not done.** Building the easy 80% and filing the hard 20% as a tracked Open Gap is still a half-implementation — making the gap *visible* doesn't make the feature *finished*. An Open Gap is legitimate only for (a) genuinely separable work that is its own feature/subsystem (e.g. the type-inference engine), or (b) something needing a decision only Nicke can make — and it must be surfaced **before** building the easy half, so scope is agreed up front, not revealed after. "The harder half of what I'm building right now" is not a gap; it's unfinished work. Finish the feature's real cases — derived as well as base, the common collection/correlated forms — not just the convenient path. The only descoping Nicke has actually cleared is **transpilation**; **type inference** and **lineage** were never cleared (a prior edit wrongly listed them — corrected 2026-06-06) and are open, not Out. Everything else not yet built is an Open Gap, not an exclusion.
- **Drive to completion; do not checkpoint.** When you take on a piece of work (e.g. "type inference"), do it **full and complete** in one continuous push — every determinable case, **wired into its consumers**, working end-to-end. No "ifs, buts, or deferred." Building an engine (inference, symbols) and leaving it unwired is not done; "done" = a consumer can use it and it delivers the value, not that a layer compiles and its unit tests pass in isolation. Do NOT stop to report progress, summarise what's left, ask "should I continue?", or pause at each commit / TDD increment / gap — just keep building the next piece. The only legitimate stops: it genuinely works end-to-end, you are blocked on a decision only Nicke can make, or Nicke redirects. The only things that may stay unbuilt are the genuinely **undeterminable** (e.g. a type with no schema) — an inherent limit, not a deferral — recorded in PLAN.md, never used as a reason to stop early.
