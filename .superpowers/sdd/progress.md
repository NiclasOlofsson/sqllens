── PARSER-GAPS WAVE (plan 2026-07-04-parser-gaps-wave.md, branch worktree-parser-gaps-wave, base 5246bd3) ──
Task 1: DONE (17efe1b), reviewed — core APPROVED (537 words added, empty changed-set, reserved boundary verified both directions, good table-driven tests). TWO items unresolved:
 - DO: reviewer says NEVER-WRONG DEFECT — `SELECT do FROM t` is valid Snowflake (DO not reserved), excluded only to hold mutated floor 332; but the flipped mutant `ORDER BY do` IS valid SQL so the floor mis-counts. Fix = add DO + re-pin floor 332→331 (documented-cause, ratchet's own "mutation can't guarantee invalidity" clause). FLOOR-DIRECTION CHANGE = FORK FOR NICLAS.
 - PIVOT/UNPIVOT: over-excluded (full id_ block; they're non-reserved so `SELECT pivot FROM t` is valid) — reviewer says accept the pragmatic full-exclusion for now BUT reclassify from "dedicated-role" to a tracked Open Gap. Minor.
 - warehouse cosmetic (already reachable via keyword rule). Minor.
Task 1: COMPLETE after one fix loop (commits 5246bd3..17efe1b core + 4e97659 fix). 538 identifier holes recovered, empty changed-set over 2,976 files, fallback ratchet held 110. DO added per Niclas ruling; mutated floor re-pinned 332→331 (documented-cause: flipped mutant `ORDER BY do` is provably-valid SQL; Niclas-approved 2026-07-04). PIVOT/UNPIVOT over-exclusion tracked as PLAN.md Open Gap (post-source-slot split deferred). Exclusion set = 7 dedicated-role + 65 engine-reserved (reviewer-verified both directions). Verified directly: DO in grammar, floor comment, PLAN gap. tier1 2498/1 (after known worker-pool flake rerun), tier2 32.
Task 2 (#13 duckdb): DONE. Empty-bound slices (`[::2]`/`[1::2]`/`[::-1]` — `::` maximal-munches to TYPECAST; added a `begin?::step?` slice sub-alt BELOW the a_expr index) + string-literal method receivers (`'abc'.upper()` — added `opt_indirection` to the `aexprconst` c_expr alt; lower applies the chain → `upper('abc')`). Slice lowering untouched (opaque-index, no fabricated bounds). Proof kit: IR hash-diff over 1,037 docs query files = EMPTY changed-set; SLL fallback 25→25 (held ratchet 25); `other` 0; negatives 333/400 + 24/24. doc-coverage 2 probes raised noparse→query (support raise). No corpus graduation (KNOWN_BAD unrelated; query bucket already 100%). tier1 2501/1-skip, tier2 32, typecheck clean. Report: task-2-report.md.
